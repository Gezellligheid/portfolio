import type { Config } from "vike/types";

export default {
  title: "Projects — Elias",
  description: "Browse the portfolio of Elias — fullstack projects built with Vue, React, TypeScript, and modern web technologies.",
} as Config;
